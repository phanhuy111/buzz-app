import { LayoutDefault } from "components";
import { Text } from "react-native";

const Setting = () => {
  return (
    <LayoutDefault>
      <Text>Setting</Text>
    </LayoutDefault>
  );
};

export default Setting;
