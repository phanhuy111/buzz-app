import { Text } from "react-native";

import { LayoutDefault } from "components";

const Setting = () => {
    return (
        <LayoutDefault>
            <Text>Setting</Text>
        </LayoutDefault>
    );
};

export default Setting;
